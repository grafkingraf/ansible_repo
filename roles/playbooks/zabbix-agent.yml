---
- hosts: centos
  become:
    true
  become_method:
    su
  become_user:
    root
  roles:
   - zbx-agent

- hosts: ubuntu
  become:
    true
  become_method:
    sudo
  become_user:
    root
  roles:
   - zbx-agent
